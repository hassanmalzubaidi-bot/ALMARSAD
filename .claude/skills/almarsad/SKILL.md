---
name: almarsad
description: >-
  Runbook for the "مرصد الشرق الأوسط" (Middle East Observatory / ALMARSAD) geopolitical-events
  website — a self-contained static site on Netlify fed by a news pipeline. USE THIS SKILL
  whenever the user asks to update the news / الأخبار, add events, publish or deploy the
  observatory, run the admin panel (لوحة الأدمن), feature an event (إبراز ★), touch the map/
  atlas (الأطلس/الخريطة), edit the analytical report (التقرير), work on the ALMARSAD repo,
  the famous-biscochitos-e29381.netlify.app site, or the folder "موقع المرصد - نشر محدث".
  Trigger even on terse asks like «حدّث الأخبار», «شغّل الموقع», «انشر», «الروتين لم يعمل»,
  or when a famous-biscochitos-e29381 / ALMARSAD URL appears. This skill carries the exact
  commands, the compact data schema, the classification rules, the deploy path, and the
  known constraints — do not reinvent them.
---

# مرصد الشرق الأوسط — ALMARSAD Runbook

موقع رصد أحداث جيوسياسية للشرق الأوسط: **ملف واحد مكتفٍ ذاتيًا** (`public/index.html`) كل بياناته
مضمّنة داخله، يعمل على Netlify، ويتغذّى من خط أنابيب أخبار في `pipeline/`.

## الإحداثيات الثابتة (احفظها)
| البند | القيمة |
|---|---|
| مجلد المشروع | `C:\Users\roa44\OneDrive\Työpöytä\موقع المرصد - نشر محدث` |
| مستودع GitHub | `https://github.com/hassanmalzubaidi-bot/ALMARSAD` (**Public** — لضرورة نشر Netlify المجاني) |
| مشروع Netlify | `famous-biscochitos-e29381` |
| الرابط الحيّ | https://famous-biscochitos-e29381.netlify.app |
| النشر | Git-backed: **كل `git push` إلى `main` = نشر تلقائي**. مجلد النشر = `public/` فقط |
| الروتين المجدول | `trig_014AqQ6qfpuoRNYRkhVYEiZ9` — cron `0 4,16 * * *` (7ص/7م الرياض)، نموذج نشر مباشر — **الوحيد المفعّل** |
| حصاد Actions | `.github/workflows/fetch_news.yml` — cron `30 4,16 * * *` (7:30ص/7:30م الرياض)، يودع الحصاد في `main` (طبقة احتياط) |
| لوحة NEWS الشقيقة | مستودع `hassanmalzubaidi-bot/NEWS` — لوحة التوتر اليومية في `docs/`. **رابطها من التذييل أُزيل مؤقتًا** (GitHub Pages غير مفعّلة → 404). **لا تُعِد الرابط** إلا بعد تفعيل Pages (إعدادات مستودع NEWS → Pages → Deploy from branch → `main` → `/docs`) والتحقق من أن https://hassanmalzubaidi-bot.github.io/NEWS/ يستجيب — عندها أعد مفتاح `footer.daily` للقاموسين ar/en وسطر الرابط في `footerGrid` |

مجلدات النشر: `public/` منشور للجمهور · `pipeline/` و`.claude/` وكل ما عداه **خارج النشر** (لا يصله الجمهور).

---

## المهمة الأساسية: تحديث الأخبار

النموذج **نشر مباشر بلا مراجعة**. نفّذ من مجلد المشروع بالترتيب:

### ① الجلب
```bash
python pipeline/fetch_sources.py
```
يكتب `pipeline/raw_items.json` من RSS (الجزيرة، RT عربي، BBC عربي) + GDELT، مُصفّى بصلة الشرق الأوسط.
GDELT قد يفشل بـ429 من عناوين مشتركة — تابع بما توفّر من RSS.

### ② التصنيف (خطوتك أنت — لا سكربت)
اقرأ `pipeline/raw_items.json`. لكل خبر **حقيقي وذي صلة بالشرق الأوسط فقط**، أنشئ عنصرًا في مصفوفة
JSON تكتبها إلى `pipeline/classified.json` بهذه الحقول **بالضبط**:
```json
{"التاريخ":"YYYY-MM-DD","الدولة":"إيران","المدينة":"طهران",
 "طبيعة":"عسكري / نووي","نطاق":"دولي","خطر":"حرج",
 "الحدث":"جملة عربية واحدة واضحة","التفاصيل":"جملة إضافية أو \"\"","المصدر":"اسم القناة"}
```
**قواعد التصنيف (حرجة للجودة):**
- **طبيعة**: من {عسكري، سياسي، دبلوماسي، أمني، اقتصادي، إنساني، قانوني، تقني، اجتماعي، بيئي} — يمكن الدمج بـ«/». النووي يُصنّف **عسكري**.
- **نطاق**: `إقليمي` أو `دولي` أو `داخلي`.
- **خطر**: `حرج` / `مرتفع جداً` / `مرتفع` / `متوسط` / `منخفض` — احكم بحدّة الحدث.
- **الدولة**: الكيان الأساسي؛ يُسمح بالتركيب «إيران / الولايات المتحدة» أو الفاعلين «حماس»، «حزب الله»، «الحوثيون».
- **المدينة**: مدينة معروفة إن ذُكرت، وإلا `عام` أو `دولي`. المدن غير المعروفة أضِف إحداثياتها إلى `CITY_COORDS` في `pipeline/config.py`.
- **لا تختلق حدثًا أبدًا**: كل عنصر من خبر فعلي في `raw_items.json`.
- **تجاهل** غير الجيوسياسي (رياضة، فن، منوّعات، حوادث فردية، إعلانات) والمكرّر.
- اكتب `الحدث` بصياغة تحليلية مركّزة لا نسخًا حرفيًا للعنوان.

### ③④ الدمج والنشر المباشر
```bash
python pipeline/merge_rebuild.py     # يطبع «نُشر: N»
```
يضيف الجديد مباشرةً إلى `public/index.html` (إزالة تكرار ضبابية مقابل القاعدة)، ويحدّث العدّادات.
راجع «إزالة التكرار» أدناه إن ظهر «مكرر مُتجاوَز» عالٍ.

### ⑤ الدفع (ينشر تلقائيًا)
```bash
git add -A && git commit -m "تحديث إخباري: N حدثًا ليوم YYYY-MM-DD" && git push
```

### ⑥ التحقق الحيّ (إلزامي)
انتظر ~20-60 ثانية ثم تحقق أن النشر وصل:
```bash
curl -s "https://famous-biscochitos-e29381.netlify.app/?v=$(date +%s)" -o /tmp/live.html
python -c "import re,json;h=open('/tmp/live.html',encoding='utf-8').read();d=json.loads(re.search(r'<script id=\"EV\" type=\"application/json\">(.*?)</script>',h,re.S).group(1));r=d['rows'];print('إجمالي:',len(r),'| آخر تاريخ:',max(x[0] for x in r))"
```
إن لم يتغيّر الإجمالي بعد دقيقتين، افحص لوحة Netlify → Deploys (قد يكون بناء فاشل — انظر «القيود»).

---

## بنية البيانات (تنسيق الصف المضغوط `EV`)

البيانات في `public/index.html` داخل `<script id="EV" type="application/json">{...}</script>`:
مفاتيح: `ents` (أسماء الكيانات)، `kinds`، `nat` (الفئات)، `sc` (النطاقات)، `rows`، `net`، `generated`.

كل صف مصفوفة بـ 14 عنصرًا:
```
[0] التاريخ YYMMDD   [1] فهرس الكيان في ents   [2] المدينة   [3] عرض   [4] طول
[5] دقّة (1=مدينة، 0=مستوى دولة)   [6] قناع الفئات bitmask على nat   [7] النطاق 0-3
[8] الخطر 0-5 (0=حرج … 4=منخفض، 5=غير محدد)   [9] الحدث   [10] التفاصيل
[11] المصدر prov (0=أساسي، 1=مصدر مفتوح، 2=دفعة ذكاء اصطناعي)   [12] اسم المصدر
[13] تحقّق (0/1)   [14] مميّز ★ (1=بارز، اختياري)
```
سلّم ألوان الخطر (RC): `["#991b1b","#dc2626","#ea580c","#d97706","#16a34a","#64748b"]`.
لا تعدّل هذا التنسيق يدويًا — استخدم السكربتات أو لوحة الأدمن.

---

## لوحة الأدمن (إدارة المحتوى المنشور)
أداة **محلية فقط** (127.0.0.1، لا تُنشر للويب، بلا أسرار). التشغيل:
```bash
python pipeline/admin.py         # أو نقر مزدوج على admin.bat في جذر المشروع
```
تفتح على http://127.0.0.1:8765 : بحث/تصفية كل الأحداث · تعديل أي حقل · حذف · **إبراز ★/☆** ·
زر «💾 حفظ» (يكتب public/index.html محليًا) · زر «🚀 حفظ ونشر» (يكتب + commit + push → نشر تلقائي).

## ميزات الواجهة (public/index.html)
- **الأطلس / الخريطة**: تكبير/تصغير بالعجلة واللمس + أزرار (+/−/↻)، سحب، **تجميع نقاط (clustering)** عند التصغير (نقر العنقود يقرّب)، النقاط تبقى بحجم ثابت عبر CSS `--z`، **شريط زمني** يمرّر الأحداث يومًا بيوم + تشغيل ▶.
- **إبراز الأحداث ★**: الحقل `row[14]=1` → شريط ذهبي «أحداث بارزة» أعلى كل الصفحات (حتى 8، الأحدث أولًا).
- **التقرير التحليلي**: رسوم مكدّسة بألوان درجات الخطر (شكل الاتجاه الشهري + الطبائع). داشبورد لا صفحة نصية.

تعديلات الواجهة = رقعة Python دقيقة على `public/index.html` عبر بحث/استبدال نصّي مؤكَّد، ثم اختبار محلي
(`python -m http.server` + متصفّح، **مع `?nocache=` لتجاوز كاش المتصفّح**)، ثم push. rAF لا يعمل في تبويب
خفيّ — استخدم setTimeout في منطق الرسم.

---

## القيود والمزالق المعروفة (لا تكرّر اكتشافها)
1. **Netlify المجاني يحظر مساهمي Git غير الموثّقين على المستودعات الخاصة** → لهذا المستودع **عام**. لا تجعله خاصًّا إلا مع خطة Pro. أعراض الفشل: «Build blocked: Unrecognized Git contributor».
2. **دفع الروتين السحابي يعمل الآن** (ثبت بنشرات 2026-07-23 وما بعدها). إن عاد فشل «Build blocked» أو رفض الدفع، راجع ربط GitHub في claude.ai → الإعدادات.
3. **الطابور/المراجعة أُلغيت**: النموذج نشر مباشر. لا يوجد `pending.json` ولا `promote.py`.
4. **`pipeline/raw_items.json` أصبح مُتعقَّبًا في git** (يودعه سير عمل الحصاد مع `fetch_meta.json`) — **لا تتجاهله ولا تحذفه**. أما `classified.json` فما زال مؤقتًا في `.gitignore`.
5. **بيئات Claude السحابية قد تحجب مواقع الأخبار** (بوابة السياسات ترد 403 على الجزيرة/RT/BBC/GDELT في بعض البيئات — بيئة الروتين `env_01DNmVPT…` مفتوحة، وبيئات أخرى محجوبة). إن فشل `fetch_sources.py` بـ URLError في بيئة سحابية، **استخدم الحصاد المودع في المستودع**: اسحب `main` واقرأ `pipeline/raw_items.json` بعد التحقق من `pipeline/fetch_meta.json` (الصيغة: `{"fetched_at":"…Z","count":N}` — لا تنشر من حصاد أقدم من 12 ساعة أو count=0).
6. عند فحص الموقع الحيّ استخدم دائمًا `?param=` عشوائيًا لتجاوز كاش CDN.
7. **لا تنشئ روتينًا مجدولًا ثانيًا للتحديث**: الروتين `trig_014Aq…` هو الوحيد. (في 2026-07-23 أُنشئ روتين مكرر `trig_01DSE…` بالخطأ واكتُشف الازدواج وحُذف في اليوم نفسه — إزالة التكرار في `merge_rebuild.py` حمت المحتوى، لكن الازدواج يهدر جلسات ويضاعف الدفعات.)

## البنية التحتية للأتمتة (مرجع كامل — حُدّث 2026-07-23)

ثلاث طبقات تعمل معًا، كلها مؤتمتة:

1. **الروتين المجدول** `trig_014AqQ6qfpuoRNYRkhVYEiZ9` (المحرك الأساسي): جلسة Claude جديدة مرّتين
   يوميًا (7ص/7م الرياض = `0 4,16 * * *` UTC) في بيئة `env_01DNmVPT…` مفتوحة الشبكة. ينفّذ ①→⑤
   كاملة: جلب، تصنيف (هو الوكيل)، دمج، دفع مباشر إلى `main`.
2. **سير عمل الحصاد** `.github/workflows/fetch_news.yml` (طبقة احتياط وسجل): على مشغّلات GitHub
   بعد الروتين بنصف ساعة (`30 4,16 * * *` UTC). يشغّل `fetch_sources.py` ويودع
   `pipeline/raw_items.json` + `pipeline/fetch_meta.json` في `main` برسالة «حصاد أخبار آلي [skip ci]».
   فائدته: (أ) مصدر بيانات جاهز لأي بيئة محجوبة الشبكة، (ب) نبض حيّ يُظهر متى نجح الجلب آخر مرة
   وكم خبرًا توفر، (ج) سجل تاريخي للحصاد في git. يمكن تشغيله يدويًا من تبويب Actions (workflow_dispatch).
3. **Netlify**: ينشر تلقائيًا من `public/` عند كل دفعة إلى `main` خلال ~دقيقة.

**التسلسل اليومي الكامل:** 7:00 الروتين يجلب ويصنّف وينشر → 7:30 الحصاد يودّع سجلّه → Netlify يحدّث
الموقع الحيّ. أي فشل في الروتين لا يُفقد اليوم: الجولة التالية (أو تشغيل يدوي بالحصاد المودع) تغطيه،
وإزالة التكرار تمنع الازدواج دائمًا.

## سجل تغييرات 2026-07-23 (ما فعلته جلسة الترتيب — اقرأه لتفهم الوضع الراهن)

| # | التغيير | أين |
|---|---|---|
| 1 | **ربط الموقعين**: رابط «لوحة التوتر الجيوسياسي اليومية ↗» في عمود «أقسام الموقع» بتذييل المرصد، بنص مترجم (`footer.daily` في قاموسي ar/en داخل `public/index.html`) يشير إلى https://hassanmalzubaidi-bot.github.io/NEWS/ | PR #1 — مدموج، **ثم أزال الأدمن الرابط لاحقًا في اليوم نفسه** لأن Pages غير مفعّلة في NEWS والرابط يعطي 404 (انظر «لوحة NEWS الشقيقة» أعلاه لشروط إعادته) |
| 2 | **رابط عكسي** من لوحة NEWS إلى المرصد: في قالب `dashboard_bot.py` (فيتجدد مع كل توليد) وفي `docs/index.html` و`docs/archive.html` المنشورين | NEWS PR #36 — **ما زال مفتوحًا بانتظار الدمج** |
| 3 | **سير عمل الحصاد** `fetch_news.yml` + رفع تجاهل `raw_items.json` + توثيق في `run_daily.md` وREADME | PR #2 — مدموج |
| 4 | **توحيد الروتينات**: اكتُشف روتين مكرر أُنشئ في الجلسة نفسها (`trig_01DSE…` على 8ص/8م) فحُذف؛ بقي `trig_014Aq…` وحده | claude.ai Routines |
| 5 | اختبار شامل: الحصاد اليدوي جلب 125 خبرًا وأودعها بنجاح؛ جولة تصنيف اختبارية شُغّلت للتحقق | سجل Actions + تاريخ git |

ملاحظات تشغيلية مترتبة: لا تُرجع `raw_items.json` إلى `.gitignore` · لا تحذف `fetch_meta.json` ·
لا تُنشئ روتينًا ثانيًا · المسار اليدوي المحلي (الخطوات ①-⑥ أعلاه) ما زال صالحًا تمامًا ومتعايشًا
مع الأتمتة بفضل إزالة التكرار.

## ملفات خط الأنابيب
`pipeline/config.py` (مصادر RSS + قناع GDELT + التصنيف الثابت + جدول الإحداثيات CITY_COORDS/CAPITALS) ·
`fetch_sources.py` (①) · `classify.py` (مساعد ترميز حتمي: geocode/nat_mask/dedup_key) ·
`merge_rebuild.py` (③④ نشر مباشر) · `admin.py` (لوحة الأدمن) · `run_daily.md` (إجراء الروتين).

## المصادر الثلاثية للأخبار
RSS (العمود الفقري الموثوق: الجزيرة، RT عربي، BBC عربي) · GDELT (مساند، محدود المعدّل) · بحث Claude على
الويب (طبقة عمق عند الحاجة لتغطية مسرح محدد — استخدمه بباحثين متوازيين حسب الجغرافيا للفجوات الكبيرة).

> علاقة بمشاريع أخرى: القاعدة نفسها تغذّي أيضًا ملف Excel ولوحة Power BI («مركز القيادة») في مجلد
> «Power BI احترافي - الأحداث الإقليمية» — لكن ذلك خارج نطاق هذه المهارة (هذه المهارة للموقع فقط).
